# 英语句子键盘练习小程序 MVP PRD v0.3

## 一、产品概述
### 1.1 产品目标
面向中国用户的轻量化英语句子输入练习工具，通过"看释义+音标→敲英文句子"的训练模式，帮助用户提升英文打字速度、句子记忆和语感，核心价值是低门槛、碎片化、即时反馈的口语/写作基础练习体验。

### 1.2 目标用户画像
| 用户分层 | 特征 | 核心需求 |
|---------|------|----------|
| 学生群体（初中/高中/大学生） | 12-25岁，有英语学习需求，日常使用手机，需要提升词汇/句子积累 | 碎片化时间练习、贴合教材内容、即时反馈 |
| 职场人士 | 25-40岁，有商务英语/日常英语使用需求，希望提升口语流利度和写作效率 | 职场场景化内容、短平快练习、学习进度追踪 |
| 英语爱好者 | 18-35岁，主动学习英语，希望通过输入练习强化记忆 | 自定义内容导入、AI生成个性化练习、数据统计 |

### 1.3 核心场景
1. **通勤碎片化练习**：地铁/公交上利用5-10分钟完成一组10句的短句练习
2. **课后巩固练习**：学生课后针对课本课文句子进行输入练习，强化记忆
3. **自定义内容练习**：用户导入自己需要背诵的文章/句子，针对性练习
4. **学习成果回顾**：查看自己的练习统计，了解学习进度和薄弱点

## 二、MVP 范围边界
### 2.1 包含范围（第一阶段必须实现）
✅ 基础敲句练习功能：中文释义+音标展示，英文输入，实时错误提示，正确率/连击/进度计算
✅ 预制主题课程库：内置10套基础主题课程（日常口语、商务基础、校园场景等）
✅ AI生成课程：输入主题/关键词，自动生成对应练习句子集
✅ JSON课程导入：支持用户导入符合格式的JSON课程文件
✅ 本地学习统计：练习时长、正确率、最高连击、连续学习天数本地存储展示
✅ 微信授权登录：支持微信一键登录，登录后数据云同步
✅ 响应式适配：兼容主流手机屏幕尺寸，中文界面为主

### 2.2 排除范围（第一阶段不实现，后续迭代考虑）
❌ 用户社区/社交功能
❌ 付费会员/课程售卖体系
❌ 音频发音/听力练习功能
❌ 多设备实时同步（仅支持登录时全量同步）
❌ 错题本/复习功能
❌ 好友排行榜/对战功能
❌ ZIP格式课程导入（仅支持JSON）
❌ 自定义键盘布局/皮肤

## 三、关键用户流程
### 3.1 敲句练习流程
```
用户进入小程序 → 选择课程 → 进入练习页面 → 展示当前句中文释义+音标 → 用户输入英文 → 
系统实时校验：
  → 输入正确：自动进入下一句，连击+1，进度更新
  → 输入错误：错误位置标红，提示正确拼写，连击清零
→ 完成所有句子 → 展示本次练习统计（正确率、时长、连击）→ 可选"再练一次"或"返回课程列表"
```

### 3.2 课程选择流程
```
用户进入首页 → 展示推荐课程/我的课程 → 点击课程卡片 → 查看课程详情（句子数量、难度、主题）→ 
点击"开始练习" → 进入练习流程
```

### 3.3 AI生成课程流程
```
用户进入AI生成页面 → 输入主题/关键词（如"职场面试常用句"）→ 选择难度（简单/中等/困难）→ 
选择句子数量（10/20/30句）→ 点击生成 → 系统生成句子集 → 预览课程 → 保存到"我的课程" → 开始练习
```

### 3.4 课程导入流程
```
用户进入导入页面 → 下载JSON模板 → 按照模板格式填写内容 → 上传JSON文件 → 系统校验格式：
  → 校验通过：导入成功，保存到"我的课程"
  → 校验失败：提示错误位置，引导修改后重新上传
```

### 3.5 登录同步流程
```
用户未登录状态 → 点击"我的"页面 → 点击"微信登录" → 弹出微信授权弹窗 → 用户确认授权 →
系统同步本地已有数据到云端 → 后续练习数据自动同步到云端
```

### 3.6 学习统计流程
```
用户进入"统计"页面 → 展示：
  - 累计练习时长
  - 总正确率
  - 最高连击记录
  - 连续学习天数
  - 最近7天练习趋势图
→ 可点击查看详细练习历史记录
```

## 四、数据结构设计
### 4.1 本地存储数据（仅保存在用户设备，未登录时使用）
| 数据类型 | 存储内容 | 持久化策略 |
|---------|----------|------------|
| 用户基础信息 | 临时用户ID、是否登录标记 | 永久存储 |
| 练习记录 | 每次练习的课程ID、正确率、时长、连击、练习时间 | 永久存储，最多保留最近1000条 |
| 自定义课程 | 用户导入/AI生成的课程内容 | 永久存储，最多保留50套 |
| 应用配置 | 音效开关、自动跳题开关、难度偏好 | 永久存储 |

### 4.2 云同步数据（登录后同步到服务器）
| 数据类型 | 同步内容 | 同步策略 |
|---------|----------|----------|
| 用户信息 | 微信openid、昵称、头像 | 登录时同步 |
| 练习统计 | 累计练习时长、总正确率、最高连击、连续学习天数 | 每次练习完成后增量同步 |
| 练习历史 | 所有练习记录 | 每次练习完成后增量同步 |
| 自定义课程 | 用户导入/AI生成的所有课程 | 新增/修改时全量同步 |
| 应用配置 | 用户个性化设置 | 修改时同步 |

## 五、微信小程序平台限制与应对
| 平台限制 | 应对方案 |
|---------|----------|
| 本地存储上限10MB | 1. 自定义课程最多保存50套，超出提示用户清理旧课程；2. 练习记录最多保留最近1000条，旧记录自动归档到云端（登录后）；3. 图片/静态资源使用CDN，不占用本地存储 |
| 网络请求超时限制（10s） | 1. AI生成课程超时后自动重试1次，仍失败提示用户稍后再试；2. 数据同步采用增量同步，减少单次请求数据量；3. 离线时支持本地练习，网络恢复后自动同步 |
| 内容安全审核要求 | 1. AI生成的句子先经过内容安全接口审核，违规内容直接过滤；2. 用户导入的课程内容经过安全审核，违规内容禁止导入；3. 内置课程预先审核通过 |
| 微信授权规则 | 1. 不强制用户登录，未登录时所有功能均可使用（仅无法云同步）；2. 仅在用户主动点击登录按钮时发起授权申请，不弹窗骚扰 |
| 包体积上限2MB | 1. 静态资源（图片、字体）全部使用CDN；2. 采用分包加载，核心功能放在主包，非核心功能放在分包；3. 精简第三方依赖，优先使用小程序原生API |

## 六、异常场景处理
| 异常场景 | 处理逻辑 |
|---------|----------|
| 网络断开 | 1. 练习功能正常可用，数据保存在本地；2. 提示用户"当前网络不可用，练习数据将保存在本地，网络恢复后自动同步"；3. AI生成/课程导入/云同步功能暂时禁用，提示网络恢复后使用 |
| JSON导入格式错误 | 1. 明确提示错误位置（如"第3行缺少'english'字段"）；2. 提供格式校验示例，引导用户修改；3. 支持下载标准模板参考 |
| AI生成内容违规 | 1. 提示"生成内容包含违规信息，请更换关键词后重试"；2. 记录违规关键词，避免重复生成 |
| 登录授权失败 | 1. 提示"授权失败，请稍后重试"；2. 保留未登录状态，不影响用户继续使用本地功能 |
| 云同步冲突 | 1. 采用"最新修改优先"策略，后修改的数据覆盖旧数据；2. 同步前提示用户"将合并本地和云端数据，是否继续？" |
| 练习过程中小程序退到后台 | 1. 自动保存当前练习进度，用户再次进入时可选择继续练习或重新开始；2. 练习时长只统计前台活跃时间 |

## 七、验收标准（可测试）
### 7.1 功能类验收标准
| 验收项 | 验收标准 |
|---------|----------|
| 敲句练习功能 | 1. 正确输入句子后自动进入下一句，正确率计算正确；2. 输入错误时错误位置标红，提示正确拼写，连击清零；3. 完成练习后统计数据（正确率、时长、连击）准确 |
| 课程选择功能 | 1. 内置课程正常加载，课程详情展示正确；2. 点击"开始练习"正常进入练习页面 |
| AI生成课程功能 | 1. 输入关键词后可正常生成对应主题的句子集；2. 生成的句子符合难度要求，内容无违规；3. 生成的课程可正常保存和练习 |
| JSON导入功能 | 1. 符合格式的JSON文件可正常导入并使用；2. 格式错误的文件提示明确的错误信息；3. 导入的课程内容与文件内容一致 |
| 学习统计功能 | 1. 练习时长、正确率、连击、连续学习天数统计准确；2. 最近7天练习趋势图数据准确 |
| 登录同步功能 | 1. 微信授权登录正常；2. 登录后本地数据成功同步到云端；3. 换设备登录后可正常获取云端数据 |

### 7.2 体验类验收标准
| 验收项 | 验收标准 |
|---------|----------|
| 界面适配 | 1. 兼容iOS/Android主流机型，界面无错位、无截断；2. 中文界面清晰易懂，无错别字 |
| 响应速度 | 1. 页面切换时间≤1s；2. 输入校验实时反馈，延迟≤200ms；3. AI生成课程时间≤10s |
| 离线可用性 | 1. 无网络时本地练习、本地课程查看功能正常可用；2. 网络恢复后数据自动同步 |

### 7.3 合规类验收标准
| 验收项 | 验收标准 |
|---------|----------|
| 内容安全 | 1. AI生成/用户导入的违规内容被正确拦截；2. 无违法违规内容展示 |
| 用户隐私 | 1. 不强制收集用户信息，未登录时所有功能可用；2. 用户数据加密存储，不泄露 |

## 八、待决策项
以下事项需要确认后再进入下一阶段：
1. AI生成课程使用哪个大模型API？（备选：豆包/通义千问/OpenAI）
2. 云同步后端使用什么技术方案？（备选：微信云开发/自建FastAPI+SQLite）
3. 内置课程的具体内容和数量？（需要确认主题、难度分级、句子总量）
4. 是否需要添加练习音效反馈？（输入正确/错误的提示音）
5. 是否支持句子收藏功能？
6. 小程序的正式名称和LOGO？
7. 是否需要添加用户协议和隐私政策弹窗？
8. AI生成课程是否有次数限制？免费用户每天最多生成多少次？
