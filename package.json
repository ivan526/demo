{
  "name": "cloud-greet-server",
  "version": "1.0.0",
  "description": "云端问候页面后端服务",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --verbose"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=16"
  },
  "jest": {
    "testMatch": ["<rootDir>/tests/**/*.test.js"],
    "testEnvironment": "node"
  }
}
